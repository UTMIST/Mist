## Trying out cli

If you want to try running the cli:
- ```cd cli```
- ```go build -o ../bin/mist .```
- ```../bin/mist {parentcommand} {subcommand}``` 

To run all cli unit tests 
- ```cd cli```
- ```cd cmd```
- ```go test -v```